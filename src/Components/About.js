import React from 'react'

const About = () => {
  return (
    <div>
        <h4>About Task Tracker &nbsp; <small>(Version 1.0.0)</small></h4>
        <p>This Task Tracker is having a local server and it can save data.</p>
    </div>
  )
}

export default About